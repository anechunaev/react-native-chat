import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		flexDirection: 'row',
		alignItems: 'center',
		marginBottom: 4,
		gap: 5,
		opacity: 0.7,
	},
	text: {
		marginLeft: 8,
		fontStyle: 'italic',
	},
});

export default styles;
